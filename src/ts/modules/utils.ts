export function textAndAddition(text: string, num1: number, num2: number): string {
	return `${text} ${num1 + num2}`;
}

export function textAndSubtraction(text: string, num1: number, num2: number): string {
	return `${text} ${num1 - num2}`;
}
